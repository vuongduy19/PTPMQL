@model IEnumerable<DemoMVC.Models.HeThongPhanPhoi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenHeThong)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DiaChi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SoDienThoai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenHeThong)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DiaChi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SoDienThoai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HeThongPhanPhoiId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HeThongPhanPhoiId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HeThongPhanPhoiId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
